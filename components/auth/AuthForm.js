import { StyleSheet, Text, View } from "react-native";

const AuthForm = () => {
  return (
    <View>
      <Text>AuthForm</Text>
    </View>
  );
};

export default AuthForm;

const styles = StyleSheet.create({});
