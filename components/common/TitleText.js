import { StyleSheet, Text, View } from "react-native";

const TitleText = () => {
  return (
    <View>
      <Text>TitleText</Text>
    </View>
  );
};

export default TitleText;

const styles = StyleSheet.create({});
