import { StyleSheet, Text, View } from "react-native";
import React from "react";

const QuizAnswerOptions = () => {
  return (
    <View>
      <Text>QuizAnswerOptions</Text>
    </View>
  );
};

export default QuizAnswerOptions;

const styles = StyleSheet.create({});
