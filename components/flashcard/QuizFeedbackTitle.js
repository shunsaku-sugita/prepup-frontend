import { StyleSheet, Text, View } from "react-native";
import React from "react";

const QuizFeedbackTitle = () => {
  return (
    <View>
      <Text>QuizFeedbackTitle</Text>
    </View>
  );
};

export default QuizFeedbackTitle;

const styles = StyleSheet.create({});
