import { StyleSheet, Text, View } from "react-native";
import React from "react";

const QuizModalText = () => {
  return (
    <View>
      <Text>QuizModalText</Text>
    </View>
  );
};

export default QuizModalText;

const styles = StyleSheet.create({});
