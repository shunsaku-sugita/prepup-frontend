import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SavedJobListScreen = () => {
  return (
    <View>
      <Text>SavedJobListScreen</Text>
    </View>
  );
};

export default SavedJobListScreen;

const styles = StyleSheet.create({});
